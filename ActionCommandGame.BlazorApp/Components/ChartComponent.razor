@*@using ChartJs.Blazor.Charts
@using ChartJs.Blazor.ChartJS



@code {

    [Parameter]
    public PlayerResult Player { get; set; }

    public ChartData ChartData { get; set; }
    public ChartOptions ChartOptions { get; set; }

    protected override void OnInitialized()
    {

        ChartData = new ChartData
            {
                Labels = new List<string> { "Fuel", "Attack", "Defense" },
                Datasets = new List<ChartDataset>
        {
            new ChartDataset
            {
                Label = "Resources",
                Data = new List<double> { Player.RemainingFuel, Player.RemainingAttack, Player.RemainingDefense },
                BackgroundColor = "rgba(0, 255, 0, 0.5)",
                BorderColor = "rgba(0, 255, 0, 0.8)"
            }
        }
            };
        ChartOptions = new ChartOptions
            {
                Title = new ChartTitle
                {
                    Display = true,
                    Text = "Resources Remaining"
                }
            };
    }
}*@